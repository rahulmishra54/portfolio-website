# Replace with your backend API URL, including the /api prefix when the frontend is hosted separately.
VITE_API_BASE_URL=https://your-backend-domain.example.com/api

# Example for local development when backend runs on port 5000
# VITE_API_BASE_URL=http://localhost:5000/api
