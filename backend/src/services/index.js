// Services should contain application-specific logic and reusable helpers.
// Each service should be kept focused on a single portfolio feature or domain.

export default {
  // Example: projectService: await import('./projectService.js'),
};
